import Apod from "../models/Apod.js";

export const getApod = async (req, res) => {
    try {
        const arr = [];
        for(let i = 0; i <= 20; i++) {
            await Apod.find()
        }

    }
}